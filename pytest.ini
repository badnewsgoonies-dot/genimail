[pytest]
testpaths = tests
python_files = test_*.py
markers =
    unit: Fast pure-logic tests.
    integration: Multi-component integration tests.
    qt: Tests that exercise Qt-related logic.
    windows: Tests intended for Windows runtime behavior.
    slow: Longer-running test scenarios.
